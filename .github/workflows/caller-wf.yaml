name: caller workflow
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name to greet'
        required: true
        default: 'World'
jobs:
  call-workflow:
    uses: ./.github/workflows/called-wf.yaml@new-branch
    with:
      name: ${{ github.event.inputs.name }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.REPO_SECRETE }}