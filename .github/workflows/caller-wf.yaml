name: caller workflow
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name to greet'
        required: true
        default: 'World'
jobs:
  call-workflow:
    uses: om121200acc/repo-for-om121200acc/.github/workflows/called-wf.yaml@new-branch
    with:
      name: ${{ github.event.inputs.name }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.REPO_SECRETE }}